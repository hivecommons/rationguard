name: PR Verifier

on:
  pull_request_target:
    types: [opened, edited, synchronize, reopened]

permissions:
  checks: write
  pull-requests: read
  packages: read

jobs:
  verify:
    uses: hivecommons/infra/.github/workflows/reusable-pr-verifier.yml@1416d68a1fd5bd475b96c8ddfcffc8539bfa6b08 # main 2026-09-24
