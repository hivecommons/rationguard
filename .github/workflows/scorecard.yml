name: OpenSSF Scorecard

on:
  schedule:
    - cron: '0 6 * * 1'
  push:
    branches: [ "main" ]
  workflow_dispatch:

permissions:
  security-events: write
  contents: read
  actions: read
  id-token: write

jobs:
  analysis:
    uses: hivecommons/infra/.github/workflows/reusable-scorecard.yml@1416d68a1fd5bd475b96c8ddfcffc8539bfa6b08 # main 2026-09-24
